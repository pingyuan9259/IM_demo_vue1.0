// renderMessageSource

const RENDER_FIRST = 0 // 第一次渲染
const RENDER_INPUT = 1 // 用户输入
const RENDER_PUSH = 2  // socket 推送
const RENDER_LOAD = 3  // loadMore

export default {
  RENDER_FIRST,
  RENDER_INPUT,
  RENDER_PUSH,
  RENDER_LOAD
}
